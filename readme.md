# WeWorkers Coalition site

This is a simple static site generated with [Jekyll](https://jekyllrb.com/).

## Setup & serve

```
gem install jekyll bundler
bundle exec jekyll serve
```

Navigate to [http://localhost:4000](http://localhost:4000)